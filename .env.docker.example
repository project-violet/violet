# Copy to .env if you want to keep runtime data outside the checkout.
VIOLET_DATA_ROOT=./data

# Run fast-hsync once at startup, then wait this many seconds after each run.
HSYNC_INTERVAL_SECONDS=3600
